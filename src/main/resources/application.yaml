server:
  port: 8080

spring:
  application:
    name: student-registration

  datasource:
    url: jdbc:mysql://localhost:3306/studentdb?createDatabaseIfNotExist=true
    username: root
    password: MuditMyra
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false

  # Disable Flyway for now (since you’re not using migrations yet)
  flyway:
    enabled: false
